{
  "name": "dual-ai-review-loop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Evidence-grounded A/B AI review loop with rollback-friendly session history.",
  "scripts": {
    "start": "node src/cli.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": ">=1.50.0 <2"
  }
}
